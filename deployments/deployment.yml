apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: consignment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consignment
  template:
    metadata:
      labels:
        app: consignment
    spec:
        containers:
        - name: consignment-service
          image: eu.gcr.io/my-project-tattoor/consignment-service:latest
          imagePullPolicy: Always
          command: [
            "./consignment-service",
            "--selector=static",
            "--registry=kubernetes",
            "--server_address=:8080"
          ]
          env:
          - name: DB_HOST
            value: "mongo:27017"
          - name: UPDATED_AT
            value: "Пт фев  8 14:42:30 MSK 2019"
          ports:
          - containerPort: 8080
            name: cons-port